<!DOCTYPE html>
<html>
  <head>
    <title>YelpTravel!</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/main.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    
  </head>
  <body>

    <nav class="navbar navbar-inverse <%=navbar_transparent%>">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped (to a hamburger) for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">YelpTravel!</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            <li><a href="/destinations">Destinations</a></li>
            <li><a href="/destinations/new">Add new Destination</a></li>
          </ul>

          <ul class="nav navbar-nav navbar-right">
            <% if (!currentUser){ %>
              <li><a href="/login">Log In</a></li>
              <li><a href="/register">Sign Up</a></li>
            <% } else { %>
              <li><a>Signed in as <span class="capitalized-content"><%= currentUser.username %></span></a></li>
              <li><a href="/logout">Logout</a></li>
            <% } %>
          </ul> <!-- /.nav navbar-nav navbar-right -->

          </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
      </nav>

      <div class="container">     <!-- without putting message inside container, message will span the whole width of webpage -->
        <% if (flash_message_error && flash_message_error.length > 0) { %>
          <div class="alert alert-danger" role="alert">
            <%= flash_message_error %>
          </div>
        <% } %>
        <% if (flash_message_success && flash_message_success.length > 0) { %>
          <div class="alert alert-success" role="alert">
            <%= flash_message_success %>
          </div>
        <% } %>
      </div>
